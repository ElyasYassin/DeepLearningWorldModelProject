base_config: ../default.yaml

baselines:
  sac:
    learning_rate: 3.0e-4
    buffer_size: 1_000_000
    batch_size: 256
    total_timesteps: 1_000_000
    tb_log_name: sac
    save_path: trained_models/sac_target_tracking
